# iOS application icon

`AppIcon.appiconset` uses Xcode's modern single-size configuration. Add the folder
to the application's asset catalog; Xcode derives the smaller system variants
from the opaque 1024×1024 source.
